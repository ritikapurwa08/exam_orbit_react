import { Button } from "../components/ui/button";

export default function RootLayout() {
    return (
        <main>
            <div>
                hleo
                <Button>
                    this is the button
                </Button>
            </div>
        </main>
    )
}